import React from 'react'
import styled from 'styled-components'

const Form = () => {
  return (
    <FormStyled>
        Form
    </FormStyled>
  )
}

const FormStyled = styled.div`

`

export default Form